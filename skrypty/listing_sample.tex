\begin{listing}[htb]            % Rozpocznij listing
\begin{minted}[linenos]{python} % Wstaw fragment kodu 'python'
def greet(name):                % Zdefiniuj funkcję 'greet'
    print(f"Hello, {name}!")    % Wypisz sformatowany tekst

greet("Python")                 % Uruchom funkcję
\end{minted}                    % Zakończ fragment kodu
\caption{Przykładowy źródłowy}  % Dodaj podpis do listingu
\label{nazwa_listingu}          % Zdefiniuj nazwę listingu
\end{listing}                   % Zakończ listing
