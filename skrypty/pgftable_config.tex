\pgfplotstableset{
	every head row/.style = {    % Dla każdego nagłówka tabeli
		before row = \toprule,     % dodaj górną linię oraz
		after row = \midrule       % dodaj linię na dole
	},
	every last row/.style = {    % Dla ostatniego wiersza
		after row = \bottomrule    % dodaj dolną linię
	}
}
